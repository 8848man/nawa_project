import 'package:flutter/material.dart';

class WriteAnswer extends StatelessWidget {
  late int btnNr;

  WriteAnswer(this.btnNr);
  @override
  Widget build(BuildContext context) {
    return Text("hello");
  }
}
